const styles = () => {
  return {
    artistContainer: {
      maxWidth: "100%",
      margin: "0 auto",
      display: "flex",
      justifyContent: "flex-start"
    }
  };
};

export default styles;
